This version of disk supports waiting for the smartctl -t command
This version of disk detects hdc / hdd /sd? for which drive it should use.

Next version of disk needs to support auto commiting to git.
Next version of disk needs to run bad blocks
Next version of disk needs to support scanning /var/log/messages for problems
Next version to disk needs to track which host has which drive  
